\clearpage
\begin{table}[p]
  \centering
  \scriptsize
  \setlength{\tabcolsep}{0.8pt}
  \renewcommand{\arraystretch}{1.18}
  \hyphenpenalty=5000
  \exhyphenpenalty=5000
  \begin{tabularx}{\textwidth}{@{}P{0.12\textwidth}P{0.155\textwidth}P{0.125\textwidth}P{0.10\textwidth}P{0.115\textwidth}Y P{0.172\textwidth}@{}}
    \toprule
    \textbf{Condition} &
    \textbf{Workflow} &
    \textbf{Independent}\par\textbf{verifier} &
    \textbf{Human involvement} &
    \textbf{Scoring point} &
    \textbf{Fair-comparison controls} &
    \textbf{Current evidence state} \\
    \midrule

    \textbf{C0}\par Manual &
    An authorised practitioner completes the current workflow in the fixed information window. Every tool or model used is logged. &
    No condition-level verifier. &
    Practitioner produces the report; there is no later C3 review stage. &
    Completed manual report; timing and corrections only if authorised. &
    Same measure definitions, period, eligible input, source-access window and agreed reference answers. &
    \textbf{PLANNED / ON HOLD}\par Waiting for permission, reference answers and a fixed study plan. \\

    \addlinespace[2pt]
    \textbf{C1}\par Fixed\par single-agent &
    One extraction and report-writing route uses the shared catalogue and cleaning rules. Source records are kept. &
    No. &
    No human edits before scoring. &
    Automated C1 report before any review. &
    Shared controls at left, plus fixed schemas, prompts, code, environment and stop rules. &
    \textbf{IMPLEMENTED}\par Synthetic labelled D0 cases; mechanism evidence only. \\

    \addlinespace[2pt]
    \textbf{C2}\par Controlled\par multi-agent &
    Separate roles collect, extract and clean data. The verifier checks support before fixed report writing. &
    Yes: separate from extraction and composition. &
    No human edits before scoring. &
    Verifier-gated C2 report before human review. &
    Same shared and fixed technical controls as C1. &
    \textbf{IMPLEMENTED}\par Synthetic labelled D0 cases; mechanism evidence only. \\

    \addlinespace[2pt]
    \textbf{C3}\par C2 + HITL &
    C2 output enters the versioned named-review workflow. &
    Yes: inherited from C2. &
    Named reviewer may edit, approve or reject under the authorised protocol. &
    Approved post-review version, scored separately from pre-review C2. &
    Same shared controls and C2 set-up, plus the fixed review plan. &
    \textbf{WORKFLOW BUILT}\par Human results are planned and on hold. \\
    \bottomrule
  \end{tabularx}

  \caption{C0--C3 evaluation-condition matrix}
  \label{tab:meth-condition-matrix}

  \vspace{5pt}
  \parbox{\textwidth}{\scriptsize
    \textit{Fair-comparison rule:} conditions use the same measure definitions, reporting period,
    eligible input, source-access window and agreed reference answers. Any tool or model used in C0
    must be recorded. \textit{Exclusion:} the public-web company-research route has not run live. It
    is not a C0-C3 condition and provides engineering evidence only. \textit{Status:} developed by
    the author from the fixed project rules, with no study results or pass thresholds.}
\end{table}
\clearpage
