\clearpage
\begin{table}[p]
  \centering
  \scriptsize
  \setlength{\tabcolsep}{2.0pt}
  \renewcommand{\arraystretch}{1.06}
  \hyphenpenalty=4000
  \exhyphenpenalty=4000
  \begin{tabularx}{\textwidth}{@{}P{0.105\textwidth}P{0.135\textwidth}P{0.17\textwidth}P{0.17\textwidth}P{0.175\textwidth}Y@{}}
    \toprule
    \textbf{Connector / source} &
    \textbf{Approved function / version} &
    \textbf{Identity, cut-off and period rule} &
    \textbf{Source location and saved copy} &
    \textbf{Outcome states} &
    \textbf{Current status / limit} \\
    \midrule

    \textbf{Connector ability check} &
    Source list plus policy version; exact rules for identifiers, facts, events, file types and collection modes. Derived-data rule \emph{v2}. &
    Reviewed source-scoped identifier must belong to the company and be valid at cutoff. Cumulative facts require programme start through cutoff. &
    A fingerprint links the source and version, identifier, cut-off, programme window, requested facts and mode. Source-copy and derived-data SHA-256 values are checked on replay. &
    Before a call, \textbf{blocked} or \textbf{invalid} is different from a saved \textbf{record}, \textbf{no-record}, \textbf{unavailable} or \textbf{failure}. A rule-version mismatch stops the call. &
    \textbf{BUILT}\par\textbf{TESTED OFFLINE}. A traceable and consistent run does not prove the source is true. \\

    \addlinespace[1.5pt]
    \textbf{Companies House replay} &
    Companies House source; source list \emph{1.4.0}; policy \emph{2026-08-26.1}; offline replay. &
    Exact reviewed Companies House number. Filing, charge, incorporation and dissolution events must occur by the cut-off. Account states remain separate. &
    Write-once test bytes, checksum and source version; exact JSON locations and stable filing and charge identifiers; versioned derived-data hash. &
    Record; exact-ID no-record; unavailable source copy; invalid ID, duplicate or rule set; final failure kept separately. A blank is never changed to zero. &
    \textbf{BUILT}\par\textbf{FICTIONAL REPLAY}. No live registry result, current legal truth, complete coverage, valuation or production claim. \\

    \addlinespace[1.5pt]
    \textbf{UKRI replay} &
    UKRI source; source list \emph{1.3.0}; policy \emph{2026-08-26.1}; offline replay. &
    Exact UKRI organisation ID. Latest version available by cutoff is used. Cumulative totals require the exact programme-start--cutoff window. &
    Write-once test checksum; stable project and outcome-ID locations; the derived-fact location records the period, included projects, method and schema version. &
    Record or exact-ID no-record; invalid scheme, period or rules; failure remains separate. Missing or non-GBP amounts create clear incomplete states, never a zero or complete total. &
    \textbf{BUILT}\par\textbf{FICTIONAL REPLAY}. No live UKRI access, complete coverage, cause-and-effect or source-truth claim. \\

    \addlinespace[1.5pt]
    \textbf{Companies House API mode} &
    Read-only API mode appears in source list \emph{1.4.0}, but its live-use flag is false. Licence and terms evidence is on hold. &
    An exact reviewed number, company link and cut-off would still be required. An API key or valid number format would not give permission. &
    No approved live copy exists. A future run would need write-once response bytes, source details, exact locations, and reviewed evidence for the source and version. &
    \textbf{Blocked before connector invocation}. Invalid configuration or identity remains invalid; no live record, no-record, unavailable or failure evidence exists. &
    \textbf{HELD}\par\textbf{LIVE-UNRUN}. Code presence is not permission, official retrieval evidence, availability or production readiness. \\

    \addlinespace[1.5pt]
    \textbf{Public-web discovery / capture} &
    Separate public-web research v1: find possible web addresses and collect pages safely. It is not a registry connector or a replacement for official source approval. &
    Public case, reviewed Companies House number, fixed cut-off and agreed claim or measure scope. Downloaded text stays outside the instruction boundary. &
    Search output is only a list of possible sources. Evidence needs accessible captured bytes, checksum, publisher and web address, exact passage and a source-specific decision. &
    Possible, collected, blocked, unsupported or failed gaps remain clear. Missing, held or conflicting evidence cannot become support. No search result is not an official no-record finding. &
    \textbf{CONTROLLED ROUTE}\par\textbf{NOT USED IN A LIVE STUDY}. Stop controls and local checks do not prove coverage, source truth, quality or live availability. \\
    \bottomrule
  \end{tabularx}

  \caption{Fixed source connectors and their current states}
  \label{tab:sys-adapter-capability-state}

  \vspace{4pt}
  \parbox{\textwidth}{\scriptsize
    \textit{State rule:} ``record'' means a successful collection containing an accepted
    record; it is not a truth label. No-record, unavailable, blocked, invalid and failure retain
    different meanings and reporting consequences. \textit{Source note:} Developed by Faye Niu
    from versioned repository rules and local fictional tests. Nikiforova et al.
    (2020, PDF pp.~1--3, 15--17) supports use-case-specific quality checks; Gao et al. (2023a,
    ALCE, PDF pp.~3--4, 10) separates traceability/citation support from answer correctness; NIST
    (2023, PDF pp.~33--35) supports documented test conditions, uncertainty and limits. These
    sources do not certify a connector, publisher record or live-source result. The matrix is a
    design summary, not a coverage, quality, availability or production result.}
\end{table}
\clearpage
