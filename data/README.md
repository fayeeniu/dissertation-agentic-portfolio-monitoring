# Local data boundary

This directory is intentionally ignored except for this notice. Imported workbooks,
restricted portfolio data, immutable raw snapshots, databases, and report exports must
remain under ignored local runtime storage (the default is `var/`). Only fictional,
labelled synthetic fixtures belong in `fixtures/`.

